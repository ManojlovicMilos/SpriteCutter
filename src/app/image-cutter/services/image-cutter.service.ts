import { Injectable, Signal } from '@angular/core';

@Injectable({
    providedIn: 'root',
})
export class ImageCutterService {
    // public currentConfig: Signal<SpritesetConfig>;

    public constructor() {
        // this.currentConfig = 
    }
}
